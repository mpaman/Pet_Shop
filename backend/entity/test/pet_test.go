package test

import (
	"testing"

	// "github.com/mpaman/petshop/entity"
	// . "github.com/onsi/gomega"
)

func TestPetValidation(t *testing.T) {
	// g := NewGomegaWithT(t)

	// t.Run("age must be non-negative", func(t *testing.T) {
	// 	pet := entity.Pet{
	// 		Name:   "Buddy",
	// 		Age:    -1, // Invalid
	// 		Gender: "male",
	// 	}

	// 	err := pet.Validate()
	// 	g.Expect(err).NotTo(BeNil())
	// 	g.Expect(err.Error()).To(Equal("Age must be greater than or equal to 0"))
	// })

	// t.Run("valid pet", func(t *testing.T) {
	// 	pet := entity.Pet{
	// 		Name:   "Buddy",
	// 		Age:    3,
	// 		Gender: "male",
	// 		Weight: 5.5,
	// 		Vaccinated: "yes",
	// 	}

	// 	err := pet.Validate()
	// 	g.Expect(err).To(BeNil())
	// })
}
